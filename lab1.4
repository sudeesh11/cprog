#include<stdio.h>
#include<conio.h>
int main()
{
	float p,t,r;
	printf("Enter values of principle, rate and time.");
	scanf("%f %f %f", &p, &t, &r);
	printf("\n The Simple Interest is %f",(p*t*r)/100);
	return 0;
	
}
